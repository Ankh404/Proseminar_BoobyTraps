extends Node2D

# get_child(0) -> UI
# get_child(1) -> Backend



# Called when the node enters the scene tree for the first time.
func _ready():
	#TODO: set up the menu
	
	
	# run the mbfs
	#get_child(1).get_child(0).parseInputFile()
	#get_child(1).run_mbfs()
	
	
	
	# debug Backend
	#get_child(1).debug()
	
	# debug UI
	#get_child(0).debug()
	
	pass
